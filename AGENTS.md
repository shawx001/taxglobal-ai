# AGENTS.md — TaxGlobal AI 的 AI 协作铁律（Codex / Claude 每次开工先读）

> 这是上线级税务产品。下列为 **Always 规则**（生成任何代码前必遵守）。深层细节见 `docs/`。
> 开工先读：`/ARCHITECTURE.md`（抗压边界）、`docs/feature_status.md`（进度/在哪）、本步对应的 `docs/*_design_*.md`。

## A. 数值精确第一（本产品的存在性约束）
1. 金额计算**到分**正确;算错一分 = 用户报错税 = 法律/赔付风险。
2. **税率/阈值/扣除只来自版本化数据** `data/tax_years/YYYY/*.json`;**引擎里永不裸写税率/阈值**。
3. **加新税年 / 新州 = 只加数据,不改引擎逻辑**(开闭原则;2026 升级已验证此模式)。
4. 每个数**可溯官方源**(IRS Rev.Proc./州 DOR),数据文件带 `source_ids` + 归档原件。
5. 不能精确建模的 → 标进 `assumptions` 或返回 `not_covered`;**绝不编造确定数字**。

## B. 防屎山 / 架构纪律
6. 引擎 = **纯函数 + Decimal + 模块化包**(`engine/` 各领域模块,门面在 `engine/__init__.py`)。**禁单文件巨石**——曾有 1820 行上帝文件,已拆,别再堆回去。
7. 单文件 / 单函数**保持聚焦单一职责**;函数过长、文件过大要拆。**接口先行**(先定函数契约/设计文档,再实现)。
8. 数据驱动,**不写按州名/按个案的特判分支**;新规则=声明式数据 + 通用解释器。
9. **最小改动**:只动相关文件,不顺手重构无关代码。重构须**行为不变**(golden 全绿即证)。

## C. 上线工程(抗高并发)
10. 请求处理**无状态**、引擎**无共享可变状态**(横向扩展安全);热路径无磁盘/网络 IO;热只读数据缓存(规则已 `@lru_cache`)。
11. **幂等**(M2+ 写库/知识库摄取/外部调用):重试/重复提交不重复计、不损坏(幂等键/upsert/dedup)。
12. **防御性输入**:空 vs 显式 0 要区分(空=省略,0=传 0);负数夹零;超范围 → 422 / invalid_input;**绝不静默算错数**;响应取值前判空。
13. **PII / 数据驻留 + LLM 边界**:SSN/收入等加密传输+存储;日志/错误**不得泄露 PII**;Copilot 语言层可调外部 LLM API,但**调用前必须经 PII sanitizer 脱敏**、**税务计算与金额 100% 本地规则引擎、绝不经过 LLM**、Guardrail 验证 LLM 未篡改引擎数字;涉及金额的回答必须来自规则引擎(guardrail)。详见 `docs/llm_integration_reference.md`。

## D. 不自我认证 → 硬门禁(开 PR 前全绿)
14. AI 口头"应该没问题"**不算验证**。把可测的都变成硬门禁:
    - `ruff check`、`python -m unittest discover -s tests`、数据校验脚本、`git diff --check` 全绿;
    - **根 `index.html` 冻结**(hash 不变),`frontend/index.html` 仅按本步改动;
    - 新增/改动逻辑有 golden / 单测覆盖(正常 + 异常分支);CI 绿。
15. 一步一 PR(小、可验证);语义化 commit;PR 首个 commit 用 step-0 提交未跟踪的设计/标准文档。

## E. 协作分工
16. **Shaw** 拍板目标/边界/验收,**合并由 Shaw**(AI 不擅自 merge)。**Codex** 主实现 + 开 PR。**Claude** 规划/设计文档/逐行 review + **独立逐分重算**(自建参考,不只对引擎自身)+ 官方源交叉核 + 多智能体审查矩阵(`docs/code_review_matrix.md`,以 ARCHITECTURE.md 为边界)。
17. review 只报中/高/致命级,**禁命名/格式 Nitpick**;发现的真实隐患写进对应文档/记忆。
18. **写码即带工业级 sense、一次写对**(防御性输入/数值到分/数据驱动/无状态/幂等)——**不指望后续 code review 纠错**;review 是安全网,不是主纠错手段。
19. **审查节奏(省 token)**:完整多 agent 会审(含税务专家)**每个大 block 完成后跑一次**;平时小任务/小 PR 单条 review 即可。
