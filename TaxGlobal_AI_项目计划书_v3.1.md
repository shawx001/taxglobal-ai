# TaxGlobal AI — 项目计划书

**文档版本：** v3.1
**创建日期：** 2026-06-01
**最近更新：** 2026-06-02
**作者：** Shaw
**状态：** 交互原型（Clickable Prototype）完成 → 进入后端工程化阶段
**配套文档：** 《TaxGlobal AI — PRD v1.1》

---

## 〇、版本变更说明（v2.0 → v3.0）

| 项 | v2.0（规划阶段） | v3.0（当前） |
|----|----------------|-------------|
| 阶段 | 产品设计 + 架构设计完成，后端待启动 | **高保真可交互原型完成**，全部核心流程可点可算 |
| 前端 | 四个静态页面原型 | 单页应用，用户类型驱动个性化 + 9 大模块全部可交互 |
| 税额计算 | 设计稿 | **真实规则引擎**：2025 美国联邦/州税率、6 国模型、RSU/自雇/FEIE/加密均实时计算 |
| AI 助手 | 设计稿 | 接入真实 LLM（Claude）+ 全档案上下文 + 本地兜底库 |
| 电商 Nexus | 设计稿 | 平台连接 + marketplace facilitator 区分 + 自行申报义务的真实阈值/触发周数引擎 |
| 重点新增 | — | 多重身份合并、"你最需要知道的事"优先级面板、Google 登录（演示）、美国分州税 |

> 本次迭代的核心结论：**MVP 的"理解层 + 表达层 + 个性化逻辑"已用前端验证完毕，剩余工作是把规则引擎与 Agent 后端化、接真实数据源、补齐训练闭环与合规。**

### v3.1 关键产品决策（2026-06-02 评审确定）

1. **AI Copilot 语言层用外部 LLM API（2026-06-08 修订，撤销原"不接第三方 LLM"约束）。** 意图分类 + 自然语言表达由外部 LLM API（GPT-4o-mini，备选 Claude Haiku / DeepSeek）驱动，**调用前经 PII sanitizer 脱敏**；答案内容仍全部检索自**自有税务知识库**（KB / GraphRAG），可控、可审计、附法条来源；**税务计算与金额 100% 本地规则引擎、绝不经过 LLM**，Guardrail 验证 LLM 未篡改引擎数字。权威定义见 `docs/llm_integration_reference.md`。
2. **所有建议/结论都源于自有知识库。** 不依赖外部实时调用；KB 命中即返回，附来源标签与法条，毫秒级检索。
3. **第三方连接采用 OAuth 跳转模式。** 电商（Amazon 等）走"跳转到平台登录授权页 → 授权后跳回 TaxGlobal"的标准 OAuth Redirect 流程；原型已模拟该跳出/跳回体验。
4. **登录方式：Google + Apple + 微信（国内）。** 三者均以各自官方授权方式接入；原型已加三入口（演示）。
5. **首发策略：功能优先。** 先把核心功能做完整再定增长打法，个人线 vs 电商线的增长定位延后决策。

---

## 一、项目概述

### 产品定位

TaxGlobal AI 是一个面向全球用户的 **AI 驱动税务计算与合规平台**，支持个人所得税计算、电商卖家销售税合规、数字游民居住地优化、跨国税负对比，并具备自主学习与持续迭代能力。

### 一句话描述

> 输入收入，AI 实时解析当地税法，生成精确税额与法律依据，越用越聪明。

### 北极星指标（North Star）

> **每月由平台主动识别并被用户采纳的"可执行节税/合规行动"数量。**
> 不是"算了多少次税"，而是"帮用户真正省了多少钱、避了多少罚款"。

---

## 二、竞品对比与核心差异化（沿用 v2.0，无变化）

| 维度 | TaxGlobal AI | TurboTax | TaxJar | Avalara | Koinly |
|------|-------------|---------|-------|--------|-------|
| 国家覆盖 | 47+ 国家 | 仅美国 | 仅美国 | 美国为主 | 多国（仅加密）|
| AI 原生架构 | LLM + Agent | 规则引擎 | 规则引擎 | 规则引擎 | 无 |
| 法律条款引用 | 每项结论附法条 | 无 | 无 | 无 | 无 |
| 数字游民居住地优化 | 城市排名 | 无 | 无 | 无 | 无 |
| 双重征税协定查询 | 知识图谱检索 | 无 | 无 | 无 | 无 |
| 自主学习 / Trace 回流 | SFT + LoRA | 无 | 无 | 无 | 无 |

**四大差异化：** ①全球第一个数字游民税务优化平台；②Agent 多步推理 + 主动预警；③每项结论附法条、可审计；④用户数据飞轮，越用越准。

---

## 三、当前进度（Completed / In-Progress / Not-Started）

### ✅ 已完成（本轮交付）

**产品与体验**
- [x] 用户类型驱动的个性化引导（6 类纳税人：科技员工 / 自雇 / 数字游民 / 电商卖家 / 加密 / 企业主）
- [x] 多重身份组合：同一用户可叠加多身份，收入分桶（W-2 / 经营 / 加密利得 / 公司利润）并合并计税
- [x] 仪表盘"你最需要知道的事"优先级面板（紧急/留意/提示三级，跨身份合成）
- [x] 仪表盘跨身份"定制税务方案"（每条带法条 + 预估节省额）

**计算层（规则引擎，前端已验证）**
- [x] 美国 2025 联邦累进税（single/mfj/hoh）+ 标准/分项扣除 + FICA（社保/医保 + 附加医保税）
- [x] **美国分州所得税**：加州/纽约累进，IL/MA/CO/GA 平税，TX/FL/WA/NV 零税
- [x] 6 国对比模型：新加坡（累进）、葡萄牙 NHR、迪拜 0%、英国（含 NI）、德国
- [x] RSU/期权专项、自雇 SE 税、FEIE（§911）测算
- [x] 加密成本法对比（HIFO/FIFO/LIFO）→ Form 8949 示意
- [x] W-2 上传 → Form 1040 草稿生成（VLM 流程模拟）
- [x] 双重征税协定查询（双国选择）

**数字游民**
- [x] Remote 智选：20 城市库，按税负/宜居/成本/签证加权 **实时重排序**

**电商合规**
- [x] 多平台连接（Shopify/Amazon/eBay/Etsy）模拟 OAuth 授权 + 分州数据拉取
- [x] **Marketplace Facilitator 区分**：独立站（自行申报）vs 平台代缴
- [x] 真实 Nexus 引擎：按"自行申报义务"判定阈值、预计触发周数、应缴估算
- [x] 多平台销售拆分 + 境外卖家洞察（Nexus 看客户所在州）

**AI 助手**
- [x] 悬浮 Tax Copilot：**自有知识库检索**（不接第三方）+ 完整档案上下文 + 跨身份实时规划
- [x] 法条自动高亮（IRC §/CA R&TC/Rev. Rul. 等）+ 来源标签（KB 命中、毫秒级）

### 🟡 进行中 / 待硬化

- [ ] 计算引擎从前端 JS 迁移为后端权威服务（单一事实来源）
- [ ] 登录由演示替换为真实 OAuth：**Google + Apple + 微信**（各自官方授权 + 后端换 token）
- [ ] 电商平台由模拟数据替换为真实 API（Shopify/Amazon/eBay/Etsy），走 OAuth 跳转
- [ ] Copilot 自有模型自部署上线（当前原型用自有 KB 检索占位）

### 🔲 未开始

- [ ] 后端 API Gateway（FastAPI）、Agent Runtime（LangChain，18 Skills）
- [ ] 知识层：Neo4j 知识图谱 + GraphRAG + 向量库
- [ ] 训练闭环：Trace 回流、LoRA 微调、Eval Harness
- [ ] 多模态 VLM 自部署（Qwen-VL）
- [ ] 合规与安全：数据加密、审计日志、隐私合规（GDPR/CCPA）
- [ ] 企业税务模块（**按决议推迟至后续阶段**）

> **企业税务**：本轮明确暂缓，页面保留"预览版"占位，优先级集中在个人 / 数字游民 / 电商三条线。

---

## 四、核心功能模块（与 PRD 对应）

详见《PRD v1.0》功能需求章节。模块清单：①个人所得税（含 W-2→1040、RSU、加密、自雇）②Remote 智选 ③双重征税协定 ④电商 Nexus 合规 ⑤AI Copilot ⑥多重身份个性化 ⑦企业税务（推迟）。

---

## 五、技术架构（目标态）

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Next.js)                     │
│  引导 · 个人税 · Remote智选 · 电商 · 协定 · 悬浮AI     │
└────────────────────┬──────────────────────────────────┘
                     │ REST + WebSocket（流式）
┌────────────────────▼──────────────────────────────────┐
│                API Gateway (FastAPI)                  │
│        Auth(OAuth/OIDC) · RateLimit · Router          │
└──────┬─────────────────────────────────┬──────────────┘
       │                                 │
┌──────▼──────┐                 ┌────────▼───────────────┐
│   Agent     │                 │  Document Ingestion     │
│   Runtime   │                 │  Chunk → Embed → Store  │
│  LangChain  │                 └──────────┬──────────────┘
│  (18 Skills)│                            │
└──┬──────────┘                   ┌────────▼──────┐
   │                              │  向量库/OpenSearch │
   ├── Tax Rule Engine（权威计算，不用 LLM）
   ├── Connector Hub（电商/交易所/Google OAuth）
   ├── Redis Cache（热查询）
   ├── Trace Logger → PostgreSQL
   └── Knowledge Layer：Neo4j（税法图谱）+ GraphRAG

┌─────────────────────────────────────────────────────┐
│   Training Pipeline：Trace → SFT/LoRA → Eval → 回流    │
└─────────────────────────────────────────────────────┘
```

### 三层分工（核心原则）

- **计算层（规则引擎）**：税额必须精确，硬编码规则，LLM 不碰数字。
- **理解层（LLM/Agent）**：意图识别、法条检索、多步推理、结果解释。
- **学习层（训练闭环）**：用户纠正 → 训练信号 → LoRA → 模型迭代。

---

## 六、剩余技术准备如何实现（工程化落地方案）

> 这是 v3.0 的重点。原型已验证"算什么、怎么个性化"，下面是"怎么把它变成可上线的系统"。

### 6.1 计算引擎后端化（最高优先级）

- **现状**：税率表、扣除、FICA、州税、外国税、RSU/SE/FEIE 逻辑都在前端 JS。
- **目标**：抽成独立的 `tax-engine` 服务（Python，纯函数 + 规则表），前端只调用、不计算。
- **实现**：
  1. 把税率/扣除/阈值抽成**版本化的数据表**（`brackets/2025/us_federal.json`、`states/*.json`、`countries/*.json`），按税年存档，可回溯。
  2. 每个计算项暴露为纯函数并配**法条引用元数据**（`{value, cite, formula}`），保证可审计。
  3. **黄金测试集**：对每个场景固化输入→期望输出（含边界：高收入、零州税、附加医保税触发点），CI 必须全绿才发布。
  4. 前端与后端共用同一份测试夹具，杜绝"前端算一套、后端算另一套"。

### 6.2 AI Copilot：外部 LLM 语言层 + 自有知识库（2026-06-08 修订）

- **核心原则**：Copilot 语言层（意图分类 + 自然语言表达）由**外部 LLM API**（GPT-4o-mini，备选 Claude Haiku / DeepSeek）驱动，**调用前经 PII sanitizer 脱敏**；所有建议/结论必须**检索自自有税务知识库（KB / GraphRAG）**，做到快、可控、可审计；**税务计算与金额 100% 本地规则引擎、绝不经过 LLM**，Guardrail 验证 LLM 未篡改引擎数字。
- **检索优先**：用户提问 → 先在自有 KB（向量 + 图）做检索 → 命中条目带法条与来源标签返回（毫秒级）→ 自有模型只负责把检索结果组织成自然语言。
- LangChain 编排 **18 个 Skills**（parse_profile / calculate_income_tax / analyze_rsu / track_crypto / assess_feie / rank_nomad_cities / detect_nexus / classify_transaction / extract_w2 / generate_form / check_treaty 等）。
- **关键约束（Guardrail）**：涉及金额的回答必须来自规则引擎结果，自有模型不得自行编造数字；结论必须能回链到 KB 条目与法条；命中敏感/越权问题走拒答模板。
- 流式输出走 WebSocket，前端 Copilot 已具备消化多 block 的能力。
- **模型路线**：外部 LLM API（GPT-4o-mini 做意图分类 + 自然语言表达，备选 Claude Haiku / DeepSeek），调用前 PII 脱敏；`ENABLE_LLM=false` 时降级为关键词分类 + 模板响应。（M4 可选演进：自部署 Qwen + LoRA + vLLM，作为成本/合规优化路径，非 MVP 前提。）

### 6.3 知识层（GraphRAG）

- **Neo4j 节点**：Country / TaxLaw / TaxRate / Deduction / TaxTreaty / SpecialRegime / TaxpayerProfile；**边**：适用/规定/协定/引用/豁免。
- **检索**：向量检索（语义）+ Cypher 图查询（关系）双路融合 → LLM 生成带法条引用的回答。
- **MVP 降复杂度**：先用 NetworkX + pyvis + ChromaDB 跑通，再迁 Neo4j + OpenSearch。

### 6.4 连接器（Connector Hub）—— 把"模拟"变"真实"

| 连接器 | 协议 | 后端工作 | 备注 |
|--------|------|---------|------|
| Google 登录 | OAuth 2.0 / OIDC | Client ID、授权域名、后端换 token、建会话 | 登录入口之一 |
| Apple 登录 | Sign in with Apple | Service ID、私钥、回调域名，支持隐藏邮箱 | 登录入口之一 |
| 微信登录（国内） | 微信开放平台 网站应用 | AppID/AppSecret，后端用 code 换 access_token + openid | 国内市场必备 |
| Shopify | OAuth + Admin API | 跳转平台授权页→回调，拉订单/分州销售、Webhook 增量 | 独立站，**自行申报** |
| Amazon SP-API | LWA OAuth | 同上跳转授权流程，拉结算报告/分州销售 | **平台代缴** |
| eBay / Etsy | OAuth | 同上跳转授权 | 平台代缴 |
| 交易所（Coinbase/Binance/OKX） | API Key（只读） | 拉交易流水算成本基 | 加密税 |

- **统一 OAuth 跳转模式**：所有第三方连接（登录 + 电商）走"**跳转到平台登录授权页 → 授权后回调跳回 TaxGlobal**"的标准流程；原型已模拟跳出/跳回体验。
- **统一抽象**：电商连接器归一化为 `{platform, facilitator, salesByState, txns}`，对接前端已实现的 Nexus 引擎与平台拆分逻辑。
- **安全**：密钥后端加密存储（KMS），绝不下发前端；CORS 与速率限制在网关层。

### 6.5 多模态 VLM（文档识别）

- vision model 识别 W-2 / 1099-K / 工资条 / VAT 发票 / 银行对账单（M3 `extract_w2`）。
- **隐私边界**：与文本 LLM 同策——外部 vision model 调用前 PII 脱敏；识别出的金额仍交本地规则引擎计算，不进入任何外部模型。自部署 Qwen-VL 作为成本/合规优化的可选路径（非 MVP 前提）。
- 识别结果回填 Form 1040 草稿（前端流程已就绪），并交叉校验各字段一致性。

### 6.6 训练闭环

```
每日 02:00：拉 24h Trace → 质量筛选 → 转 SFT 格式
  → 新旧混合（新 20% / 历史 80%）→ LoRA 增量微调（1 epoch）
  → Eval Harness（overall ≥ 0.80 才上线）→ vLLM 热加载（零停机）
```
- 训练信号来源：用户纠正交易分类（最高质量）、计算引擎交叉验证（自动 ground truth）、👍/👎。

### 6.7 合规、安全与可观测性（大厂必备，v2.0 缺失项）

- **数据合规**：GDPR / CCPA；数据分级与最小化；用户数据删除与导出接口。
- **加密**：传输 TLS、静态加密、敏感字段列级加密；密钥 KMS 托管。
- **审计**：每条税务结论存"输入 + 规则版本 + 法条 + 输出"，满足企业留存。
- **可观测**：日志/指标/链路追踪（OpenTelemetry）；计算引擎错误率、Agent 时延、LLM 成本看板。
- **免责声明**：明确"信息参考、非税务/法律意见"，复杂跨境场景引导咨询专业人士。

---

## 七、技术栈

| 模块 | 生产选型 | MVP 极简选型 |
|------|---------|-------------|
| 前端 | Next.js 14 + Tailwind | 当前单页原型 / Streamlit |
| API | FastAPI + WebSocket | FastAPI |
| Agent | LangChain（18 Skills） | LangChain |
| 计算引擎 | Python 纯函数 + 版本化规则表 | 同左 |
| 知识图谱 | Neo4j | NetworkX + pyvis |
| 向量库 | OpenSearch | ChromaDB |
| 存储 | PostgreSQL | SQLite |
| 缓存 | Redis | — |
| 对话模型 | **外部 LLM API（GPT-4o-mini，PII 脱敏后调用）** | mock / 同左 |
| 知识检索 | 自有知识库 GraphRAG（KB 命中即答） | ChromaDB |
| VLM | vision model（PII 脱敏后调用；Qwen-VL 自部署为可选） | — |
| 训练 | HuggingFace PEFT + TRL，vLLM 服务 | Qwen2.5-0.5B + LoRA（CPU 可跑）|
| 部署 | Docker / ECS / K8s | Docker Compose |

---

## 八、路线图与里程碑

| 阶段 | 周期 | 交付物 | 验收口径 |
|------|------|--------|---------|
| **M0 原型** | ✅ 已完成 | 可交互高保真原型，全流程可点可算 | 本轮已交付 |
| **M1 引擎硬化** | 第 1–3 周 | tax-engine 后端服务 + 黄金测试集 + CI | 全部黄金用例通过，前后端结果一致 |
| **M2 Agent + 知识层** | 第 3–5 周 | 18 Skills + GraphRAG（MVP 版） | 复杂问答可多步推理并附法条 |
| **M3 连接器 + 多模态** | 第 5–7 周 | Google/Apple/微信 OAuth、Shopify/Amazon 真连（跳转授权）、Qwen-VL W-2 识别、自有 Copilot 模型上线 | 真实数据驱动 Nexus；W-2 自动回填 1040；Copilot 走自有模型+KB |
| **M4 训练闭环** | 第 7–8 周 | Trace 回流 + LoRA + Eval Harness | 分类准确率随回流提升，零停机更新 |
| **M5 合规与上线** | 第 9–10 周 | 安全/合规/可观测 + Demo 联调 | 通过安全检查清单，端到端 Demo 稳定 |

> 企业税务模块不纳入本 10 周路线图，列入后续阶段（Phase 2）。

---

## 九、商业模式（沿用 v2.0）

- **个人版**：免费 / $9.9/月 — 所得税计算 + Remote 智选 + 数字游民制度 + AI 问答
- **电商版**：$49/月 起（按销售额分档）— 多平台接入 + Nexus 监控 + 税表 + 主动预警
- **企业版**：$299/月 起 — 第二阶段

---

## 十、风险与缓解

| 风险 | 等级 | 缓解 |
|------|------|------|
| 税额算错（合规/口碑致命） | 高 | 规则引擎 + 黄金测试集 + 法条可审计，LLM 不碰数字 |
| 平台 API 政策/额度限制 | 中 | 连接器统一抽象，模拟数据兜底，降级可用 |
| 税法年年变 | 中 | 规则表按税年版本化；爬取 + LLM 解析 + 人工审核三层 |
| 数据隐私/合规 | 高 | VLM 自部署、数据加密、GDPR/CCPA、最小化采集 |
| 跨境税务复杂度被低估 | 中 | 明确免责；复杂场景引导专业人士；先覆盖高频路径 |
| LLM 成本 | 中 | 热查询缓存、规则引擎前置、小模型分担分类/意图 |

---

## 十一、成功指标（KPI）

- **北极星**：每月被采纳的可执行节税/合规行动数
- 计算引擎黄金用例通过率 = 100%；线上计算错误率 ≈ 0
- AI 问答 👍 率、法条引用覆盖率
- 电商：Nexus 预警提前量（提前 N 周命中触发）、漏报率
- 留存：个性化方案点击→采纳转化率；周活/月活

---

## 十二、项目现状小结

- ✅ 产品设计、架构设计、竞品分析、18 Skills 设计
- ✅ **高保真可交互原型完成**（计算引擎、个性化、AI、电商 Nexus 全部可跑）
- 🟡 计算引擎后端化、Google/电商真实接入（进行中规划）
- 🔲 Agent/知识层/训练闭环/多模态/合规（待启动）
- ⏸ 企业税务（推迟至 Phase 2）

---

*本文档记录 TaxGlobal AI v3.1 的产品与技术规划。配合《PRD v1.1》构成完整项目基线。*
*下次对话发送本文档即可恢复完整上下文。*
